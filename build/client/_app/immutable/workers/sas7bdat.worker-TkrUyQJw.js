let n=null;!async function(){try{const e=await async function(){try{return await import("https://cdn.jsdelivr.net/pyodide/v0.24.1/full/pyodide.mjs")}catch(n){throw console.error("Failed to load Pyodide module:",n),n}}();n=await e.loadPyodide({indexURL:"https://cdn.jsdelivr.net/pyodide/v0.24.1/full/",stderr:n=>console.error("Python Error:",n)}),await n.loadPackage("pandas"),self.postMessage({type:"PYODIDE_READY",taskId:"init"})}catch(e){console.error("Pyodide initialization error:",e),self.postMessage({type:"PYODIDE_ERROR",taskId:"init",error:e instanceof Error?e.message:String(e)})}}(),self.onmessage=async e=>{const{type:t,taskId:r,file:o,fileName:s}=e.data;if("PROCESS_FILE"===t){const e=performance.now();try{const t=await async function(e,t){if(!n)throw new Error("Pyodide not initialized");try{const r=t.toLowerCase().endsWith(".xpt"),o="/tmpfile"+(r?".xpt":".sas7bdat"),s=r?"xport":"sas7bdat",a=new Uint8Array(e);n.FS.writeFile(o,a);const i=await n.runPythonAsync(`\n            import pandas as pd\n            import warnings\n            import json\n            import numpy as np\n\n            def convert_bytes(obj):\n                if isinstance(obj, bytes):\n                    return obj.decode('utf-8', errors='ignore')\n                if isinstance(obj, (np.integer, int)):\n                    return int(obj)\n                if isinstance(obj, (np.floating, float)):\n                    return float(obj)\n                if isinstance(obj, (np.ndarray, list)):\n                    return [x for x in obj] if hasattr(obj, 'tolist') else obj\n                if isinstance(obj, pd.Timestamp):\n                    return obj.isoformat()\n                if pd.isna(obj):\n                    return None\n                raise TypeError(f"Object of type {type(obj).__name__} is not JSON serializable")\n\n            try:\n                # Read the SAS file using pandas with appropriate format\n                df = pd.read_sas('${o}', format='${s}')\n                df = df.where(pd.notnull(df), None)\n                \n                # Convert int32 columns to regular integers\n                for col in df.select_dtypes(include=['int32']).columns:\n                    df[col] = df[col].astype(int)\n\n                # Create the details dictionary with dataset information\n                details = {\n                    'num_rows': int(df.shape[0]),  # Convert numpy.int64 to regular int\n                    'num_columns': int(df.shape[1]),\n                    'columns': df.columns.tolist(),\n                    'dtypes': df.dtypes.astype(str).to_dict(),\n                    'summary': df.describe().replace({np.nan: None}).to_dict(),\n                    'unique_values': {col: df[col].unique().tolist() \n                                    for col in df.select_dtypes(include=['object']).columns}\n                }\n\n                # Convert the data to JSON format\n                json_data = df.to_json(orient='records', date_format='iso', \n                                     default_handler=convert_bytes)\n                                     \n                # Combine details and data into final result\n                result = json.dumps({'details': details, 'data': json_data}, \n                                  default=convert_bytes)\n            except Exception as e:\n                result = json.dumps({'error': str(e)})\n\n            result  # Return the JSON string\n        `),d=JSON.parse(i);if(d.error)throw new Error(d.error);return d.data=JSON.parse(d.data),d}catch(r){throw console.error("Processing error:",r),r}finally{try{n.FS.unlink(tempFile)}catch(o){console.warn("Cleanup error:",o)}}}(o,s),a=(performance.now()-e)/1e3;self.postMessage({type:"PROCESSING_COMPLETE",taskId:r,result:{...t,processingTime:a}})}catch(a){self.postMessage({type:"PROCESSING_ERROR",taskId:r,error:a instanceof Error?a.message:String(a),processingTime:(performance.now()-e)/1e3})}}};
